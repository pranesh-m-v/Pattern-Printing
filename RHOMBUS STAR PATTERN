#include <stdio.h>
int main()
{
    int r, c;
    scanf("%d", &r);
    scanf("%d", &c);
    for(int i=1; i<=r; i++)
    {
        for(int s = c-i; s>0; s--)
            printf(" ");
        for(int j=1; j<=c; j++)
            printf("*");
        printf("\n");
    }
    return 0;
}
