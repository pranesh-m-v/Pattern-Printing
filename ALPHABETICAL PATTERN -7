#include<stdio.h>
int main()
{
    int n;
    scanf("%d", &n);
    for(int i=n-1; i>=0; i--)
    {
        for(int j=n; j>i; j--)
        {
            printf("%c", j+64);
        }
        printf("\n");
    }
    return 0;
}
